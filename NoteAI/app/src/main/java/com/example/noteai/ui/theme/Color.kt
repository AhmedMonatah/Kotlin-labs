package com.example.noteai.ui.theme

import androidx.compose.ui.graphics.Color

// Primary — Warm Indigo
val PrimaryBlue    = Color(0xFF5C6BC0)  // Indigo 400
val PrimaryLight   = Color(0xFFE8EAF6)  // Indigo 50
val PrimaryDark    = Color(0xFF3949AB)  // Indigo 600

// Accents
val AccentGreen    = Color(0xFF26A69A)  // Teal
val AccentPurple   = Color(0xFF7E57C2)  // Purple
val AccentOrange   = Color(0xFFF4A261)  // Warm orange

// Backgrounds — warm off-white, easy on the eyes
val Background     = Color(0xFFF5F5F7)  // Apple-style light grey
val Surface        = Color(0xFFFFFFFF)  // Pure white
val SurfaceVariant = Color(0xFFEEEFF5)  // Subtle indigo-tinted card

// Text
val OnBackground   = Color(0xFF1C1C1E)  // Very dark (not pure black)
val OnSurface      = Color(0xFF1C1C1E)
val TextSecondary  = Color(0xFF6E6E73)  // Medium grey, easy to read
val OutlineVariant = Color(0xFFAAABB4)  // Light grey for hints

// Difficulty
val DifficultyEasy   = Color(0xFF2E7D32)  // Forest green
val DifficultyMedium = Color(0xFFF57C00)  // Deep orange
val DifficultyHard   = Color(0xFFC62828)  // Dark red

// Card tints per category (soft, gentle)
val TintBlue   = Color(0xFFE8EAF6)
val TintGreen  = Color(0xFFE0F2F1)
val TintPurple = Color(0xFFEDE7F6)
val TintOrange = Color(0xFFFFF3E0)
val TintRed    = Color(0xFFFCE4EC)
val TintCyan   = Color(0xFFE0F7FA)
val TintYellow = Color(0xFFFFFDE7)
val TintBrown  = Color(0xFFEFEBE9)